<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.ly.sys.core.user.dao.OperatorLastOnlineDao">
	<resultMap id="operatorlastonlineResult" type="OperatorLastOnline">
			<id property="id" column="id" javaType="Long"/>
				<result property="operatorId"  column="operator_id"	javaType="Long"/>
				<result property="username"  column="username"	javaType="String"/>
				<result property="uid"  column="uid"	javaType="String"/>
				<result property="host"  column="host"	javaType="String"/>
				<result property="userAgent"  column="user_agent"	javaType="String"/>
				<result property="systemHost"  column="system_host"	javaType="String"/>
				<result property="lastLoginTimestamp"  column="last_login_timestamp"	javaType="Date"/>
				<result property="lastStopTimestamp"  column="last_stop_timestamp"	javaType="Date"/>
				<result property="loginCount"  column="login_count"	javaType="Integer"/>
				<result property="totalOnlineTime"  column="total_online_time"	javaType="Long"/>
			</resultMap>
	
	
	<sql id="queryOperatorLastOnlineSql">
      	<where>
      	<![CDATA[
			1=1
		 ]]>
						<if test="id!=null">
			<![CDATA[
				and T.id=#{id}
			]]>
		</if>
								<if test="operatorId!=null">
			<![CDATA[
				and T.operator_id=#{operatorId}
			]]>
		</if>
								<if test="username!=null">
			<![CDATA[
				and T.username=#{username}
			]]>
		</if>
		<if test="uid!=null">
			<![CDATA[
				and T.uid=#{uid}
			]]>
		</if>
								<if test="host!=null">
			<![CDATA[
				and T.host=#{host}
			]]>
		</if>
								<if test="userAgent!=null">
			<![CDATA[
				and T.user_agent=#{userAgent}
			]]>
		</if>
								<if test="systemHost!=null">
			<![CDATA[
				and T.system_host=#{systemHost}
			]]>
		</if>
								<if test="lastLoginTimestamp!=null">
			<![CDATA[
				and T.last_login_timestamp=#{lastLoginTimestamp}
			]]>
		</if>
								<if test="lastStopTimestamp!=null">
			<![CDATA[
				and T.last_stop_timestamp=#{lastStopTimestamp}
			]]>
		</if>
								<if test="loginCount!=null">
			<![CDATA[
				and T.login_count=#{loginCount}
			]]>
		</if>
								<if test="totalOnlineTime!=null">
			<![CDATA[
				and T.total_online_time=#{totalOnlineTime}
			]]>
		</if>
								<if test="lastStopTimestamp_gte!=null">
			<![CDATA[
				and T.last_stop_timestamp>=#{lastStopTimestamp_gte}
			]]>
		</if>
								<if test="lastStopTimestamp_lte!=null">
			<![CDATA[
				and T.last_stop_timestamp<=#{lastStopTimestamp_lte}
			]]>
		</if>
                <if test="username_like!=null">
      <![CDATA[
        and T.username like "%${username_like}%"
      ]]>
    </if>
				    	</where>
   </sql>
   
   
	<select id="findById" resultMap="operatorlastonlineResult" parameterType="long">
		<![CDATA[
		SELECT
		*
		FROM sys_operator_last_online WHERE id=#{id}
		]]>
	</select>
  
	<select id="findByOperatorId" resultMap="operatorlastonlineResult" parameterType="long">
		<![CDATA[
		SELECT
		*
		FROM sys_operator_last_online WHERE operator_id=#{operatorId}
		]]>
	</select>
	
	<select id="findPageList" resultMap="operatorlastonlineResult" parameterType="map">
		<![CDATA[
			SELECT T.* FROM sys_operator_last_online T
    	]]>
    	<include refid="queryOperatorLastOnlineSql"/>
        
	</select>
	
	<select id="findCount" resultType="Integer" parameterType="map">
		<![CDATA[
        	SELECT COUNT(1) FROM sys_operator_last_online T
    	]]>
    	<include refid="queryOperatorLastOnlineSql"/>
	</select>
	
	<insert id="insert" parameterType="OperatorLastOnline">
		<![CDATA[
			INSERT INTO sys_operator_last_online(
												id,
												operator_id,
												username,
												uid,
												host,
												user_agent,
												system_host,
												last_login_timestamp,
												last_stop_timestamp,
												login_count,
												total_online_time
									)VALUES(
								#{id},
												#{operatorId},
												#{username},
												#{uid},
												#{host},
												#{userAgent},
												#{systemHost},
												#{lastLoginTimestamp},
												#{lastStopTimestamp},
												#{loginCount},
												#{totalOnlineTime}
									)
		]]>
		<selectKey resultType="long" keyProperty = "id">
	     	 SELECT LAST_INSERT_ID() AS ID
	    </selectKey>
	</insert>
	
	<update id="update" parameterType="OperatorLastOnline">
		<![CDATA[
			UPDATE sys_operator_last_online SET
														operator_id=#{operatorId},
																username=#{username},
																uid=#{uid},
																host=#{host},
																user_agent=#{userAgent},
																system_host=#{systemHost},
																last_login_timestamp=#{lastLoginTimestamp},
																last_stop_timestamp=#{lastStopTimestamp},
																login_count=#{loginCount},
																total_online_time=#{totalOnlineTime}
										WHERE id=#{id}
		]]>
	</update>
	
	<delete id="delete" parameterType="long">
		<![CDATA[
			DELETE FROM sys_operator_last_online WHERE id=#{id}
		]]>
	</delete>
</mapper>
